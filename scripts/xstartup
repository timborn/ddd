#!/bin/sh

echo "### executing $0"

# this is normally generated by vncserver and resides in $HOME/.vnc/xstartup, but the generated one
# include a line to KILL the process we just started.  Not good!
# Now everytime you start vncserver you must include -xstartup $HOME/scripts/xstartup

unset SESSION_MANAGER
unset DBUS_SESSION_BUS_ADDRESS
/etc/X11/xinit/xinitrc

### vncserver -kill $DISPLAY	### really a bad idea!

